eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8762/eureka/

spring:
  application:
    name: book_service
  datasource:
    url: jdbc:mysql://mysql-author:3306/author_db
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

server:
  port: 9998

logging:
  level:
    root=DEBUG:

